<UserControl
    x:Class="Logikfabrik.Overseer.WPF.Client.Views.EditConnectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cal="http://www.caliburnproject.org"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <ScrollViewer Style="{StaticResource ScrollViewerDefaultStyle}">
        <StackPanel Margin="25">
            <StackPanel>
                <ContentControl
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    cal:View.Model="{Binding Settings, Mode=OneWay}"
                    Focusable="False" />
                <StackPanel Margin="0,0,0,20">
                    <Button
                        x:Name="TryConnection"
                        Content="Try connection"
                        IsEnabled="{Binding Settings.IsDirty, Mode=OneWay}"
                        Style="{StaticResource ButtonDefaultStyle}" />
                    <TextBlock Text="Error" Visibility="{Binding ConnectionTask.Status, Converter={StaticResource TaskStatusFaultedToBooleanConverter}}" />
                    <ScrollViewer Style="{StaticResource ScrollViewerDefaultStyle}">
                        <ItemsControl Focusable="False" ItemsSource="{Binding Settings.ProjectsToMonitor, Mode=OneWay}">
                            <ItemsControl.ItemContainerStyle>
                                <Style>
                                    <!--  ReSharper disable once Xaml.RedundantPropertyTypeQualifier  -->
                                    <Setter Property="FrameworkElement.Margin" Value="0 0 0 5" />
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ContentControl cal:View.Model="{Binding}" Focusable="False" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button
                    x:Name="EditConnection"
                    Margin="0,0,2,0"
                    Content="OK"
                    IsDefault="True"
                    IsEnabled="{Binding Settings.IsNotDirty, Mode=OneWay}"
                    Style="{StaticResource ButtonDefaultStyle}" />
                <Button
                    x:Name="ViewConnections"
                    Content="Cancel"
                    IsCancel="True"
                    Style="{StaticResource ButtonDefaultStyle}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>