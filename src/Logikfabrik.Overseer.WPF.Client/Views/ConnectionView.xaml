<UserControl x:Class="Logikfabrik.Overseer.WPF.Client.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Logikfabrik.Overseer.WPF.Client.Views"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:userControls="clr-namespace:Logikfabrik.Overseer.WPF.Client.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Expander>
        <Expander.Header>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="Name"
                               Grid.Column="0" 
                               FontSize="22"
                               Text="Name" />
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Edit" />
                        <Button Content="Remove" />
                    </StackPanel>
                </Grid>
                <userControls:BusyIndicator Grid.Column="1" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>
        </Expander.Header>
        <Expander.Content>
            <ItemsControl Grid.Row="1" 
                          Focusable="False" 
                          ItemsSource="{Binding ProjectBuildViewModels, IsAsync=True}">
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="FrameworkElement.Margin" Value="5" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentControl Focusable="False" 
                                        cal:View.Model="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Expander.Content>
    </Expander>
</UserControl>
