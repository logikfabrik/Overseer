<UserControl x:Class="Logikfabrik.Overseer.WPF.Client.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:c="clr-namespace:Logikfabrik.Overseer.WPF.Controls;assembly=Logikfabrik.Overseer.WPF"
             xmlns:s="clr-namespace:Logikfabrik.Overseer.WPF.Styles;assembly=Logikfabrik.Overseer.WPF"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="TextBlockStyle"
               TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" 
                    Value="#FFFFFFFF" />
            <Setter Property="TextWrapping" 
                    Value="Wrap" />
        </Style>
        <Style x:Key="ConnectionNameTextBlockStyle" 
               BasedOn="{StaticResource TextBlockStyle}" 
               TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" 
                    Value="16" />
            <Setter Property="FontWeight" 
                    Value="Bold" />
            <Setter Property="TextAlignment" 
                    Value="Center" />
            <Setter Property="Margin" 
                    Value="10 5" />
        </Style>
        <Style x:Key="ConnectionBorderStyle"
               TargetType="{x:Type Border}">
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <SolidColorBrush Color="#FF595959" />
                </Setter.Value>
            </Setter>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#FF525252" />
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ConnectionHeaderBorderStyle"
               BasedOn="{StaticResource ConnectionBorderStyle}"
               TargetType="{x:Type Border}">
            <Setter Property="BorderThickness" 
                    Value="1" />
            <Setter Property="Padding" 
                    Value="10 0" />
        </Style>
        <Style x:Key="ConnectionContentBorderStyle"
               BasedOn="{StaticResource ConnectionBorderStyle}"
               TargetType="{x:Type Border}">
            <Setter Property="BorderThickness" 
                    Value="1 0 1 1" />
            <Setter Property="Padding" 
                    Value="10 10 10 0" />
        </Style>
    </UserControl.Resources>

    <Expander Style="{StaticResource ExpanderDefaultStyle}">
        <Expander.Header>
            <Border Style="{StaticResource ConnectionHeaderBorderStyle}">
                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <c:Icon 
                                    Height="10"
                                    Width="10"
                             Foreground="#FFFFFFFF"
                                    Data="{StaticResource IconArrowUp}"  />
                    <TextBlock Grid.Column="1" 
                               Style="{StaticResource ConnectionNameTextBlockStyle}"
                               Text="{Binding Name, Mode=OneTime}" />
                    <c:Icon Grid.Column="2" 
                                    Height="10"
                                    Width="10"
                             Foreground="#FFFFFFFF" Data="{StaticResource IconMenu}"  />
                </Grid>
            </Border>
        </Expander.Header>
        <Expander.Content>
            <Border Style="{StaticResource ConnectionContentBorderStyle}">
                <ItemsControl Focusable="False" 
                              ItemsSource="{Binding ProjectBuildViewModels, IsAsync=True}">
                    <ItemsControl.ItemContainerStyle>
                        <Style>
                            <Setter Property="FrameworkElement.Margin" 
                                    Value="0 0 0 10" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ContentControl Focusable="False" 
                                            cal:View.Model="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Border>
        </Expander.Content>
    </Expander>
</UserControl>