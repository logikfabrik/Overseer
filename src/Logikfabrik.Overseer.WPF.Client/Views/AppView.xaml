<Window x:Class="Logikfabrik.Overseer.WPF.Client.Views.AppView"
        Style="{StaticResource WindowDefaultStyle}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Window.Resources>
        <Style x:Key="TextBlockStyle"
               TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" 
                    Value="#FFFFFFFF" />
            <Setter Property="TextWrapping" 
                    Value="Wrap" />
        </Style>
        <Style x:Key="ViewNameTextBlockStyle" 
               BasedOn="{StaticResource TextBlockStyle}" 
               TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" 
                    Value="16" />
            <Setter Property="FontWeight" 
                    Value="SemiBold" />
            <Setter Property="TextAlignment" 
                    Value="Center" />
            <Setter Property="Margin" 
                    Value="10 5" />
        </Style>
        <Style x:Key="MenuBorderStyle"
               TargetType="{x:Type Border}">
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <SolidColorBrush Color="#FF595959" />
                </Setter.Value>
            </Setter>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#FF525252" />
                </Setter.Value>
            </Setter>
            <Setter Property="BorderThickness" 
                    Value="0 0 0 1" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Style="{StaticResource MenuBorderStyle}">
            <StackPanel>
                <TextBlock x:Name="ViewName"
                           Style="{StaticResource ViewNameTextBlockStyle}" />
                <Button x:Name="ViewConnections"
                        Content="Connections" />
                <Button x:Name="EditSettings"
                        Content="Settings" />
            </StackPanel>
        </Border>
        <ContentControl Grid.Row="1" 
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Focusable="False"
                        x:Name="ActiveItem" />
    </Grid>
</Window>
