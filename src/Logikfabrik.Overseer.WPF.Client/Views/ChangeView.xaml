<UserControl x:Class="Logikfabrik.Overseer.WPF.Client.Views.ChangeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBlock x:Name="Comment"
                   FontSize="14" />
        <UniformGrid Style="{StaticResource UniformGrid3To1Style}">
            <TextBlock Text="{Binding Changed, Mode=OneTime}"
                       ToolTip="{Binding Changed, Mode=OneTime}"
                       FontSize="10" />
            <TextBlock Text="{Binding ChangedBy, Mode=OneTime}"
                       ToolTip="{Binding ChangedBy, Mode=OneTime}"
                       FontSize="10" />
            <TextBlock Text="{Binding Id, Mode=OneTime}"
                       ToolTip="{Binding Id, Mode=OneTime}"
                       FontSize="10" />
        </UniformGrid>
    </StackPanel>
</UserControl>
