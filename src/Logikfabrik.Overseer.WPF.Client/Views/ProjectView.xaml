<UserControl
    x:Class="Logikfabrik.Overseer.WPF.Client.Views.ProjectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cal="http://www.caliburnproject.org"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Logikfabrik.Overseer.WPF.ViewModels.DesignTime;assembly=Logikfabrik.Overseer.WPF"
    cal:Bind.AtDesignTime="True"
    d:DataContext="{d:DesignInstance d:Type=vm:ProjectViewModel,
                                     d:IsDesignTimeCreatable=True}"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            <Setter Property="TextWrapping" Value="NoWrap" />
        </Style>
    </UserControl.Resources>
    <StackPanel Background="#FF595959">
        <TextBlock
            FontSize="20"
            FontWeight="SemiBold"
            Text="{Binding Name, Mode=OneWay}" />
        <TextBlock Text="Summary:" />
        <ContentControl
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            cal:View.Model="{Binding Digest, Mode=OneWay}"
            Focusable="False" />
        <TextBlock Text="Latest build:" />
        <ItemsControl ItemsSource="{Binding Builds, Mode=OneWay}">
            <ItemsControl.Template>
                <ControlTemplate>
                    <ScrollViewer Height="180">
                        <ItemsPresenter />
                    </ScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>
        </ItemsControl>
    </StackPanel>
</UserControl>