<UserControl
    x:Class="Logikfabrik.Overseer.WPF.Client.Views.RemoveConnectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <ScrollViewer Style="{StaticResource ScrollViewerDefaultStyle}">
        <StackPanel Margin="25">
            <StackPanel Margin="0,0,0,20">
                <TextBlock
                    FontSize="24"
                    Style="{StaticResource TextBlockDefaultStyle}"
                    Text="Remove connection" />
                <TextBlock Style="{StaticResource TextBlockDefaultStyle}" Text="Are you sure you want to remove the connection?" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button
                    x:Name="RemoveConnection"
                    Margin="0,0,2,0"
                    Content="Yes"
                    IsDefault="True"
                    Style="{StaticResource ButtonDefaultStyle}" />
                <Button
                    x:Name="ViewConnections"
                    Content="Cancel"
                    IsCancel="True"
                    Style="{StaticResource ButtonDefaultStyle}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>