<UserControl x:Class="Logikfabrik.Overseer.WPF.Client.Views.BuildView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:s="clr-namespace:Logikfabrik.Overseer.WPF.Styles;assembly=Logikfabrik.Overseer.WPF"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <SolidColorBrush x:Key="UserControl.Static.Background" 
                         Color="{x:Static s:CommonColors.ControlStaticLightBackground}" />
    </UserControl.Resources>
    <StackPanel>
        <StackPanel Margin="10 5 10 10">
            <WrapPanel>
                <TextBlock Text="{Binding VersionOrNumber, Mode=OneTime}"
                           Style="{StaticResource TextBlockHeading2DefaultStyle}" />
                <TextBlock Text="{Binding Branch, Mode=OneTime}"
                           Style="{StaticResource TextBlockHeading2DefaultStyle}" />
            </WrapPanel>
            <TextBlock x:Name="Description" 
                       Style="{StaticResource TextBlockDetailDefaultStyle}" />
        </StackPanel>
        <ItemsControl Focusable="False" 
                      ItemsSource="{Binding ChangeViewModels, Mode=OneTime}"
                      Background="{StaticResource UserControl.Static.Background}">
            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="FrameworkElement.Margin" 
                            Value="10" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentControl Focusable="False" 
                                    cal:View.Model="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>
