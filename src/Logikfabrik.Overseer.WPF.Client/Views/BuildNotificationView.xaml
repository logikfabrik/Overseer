<UserControl x:Class="Logikfabrik.Overseer.WPF.Client.Views.BuildNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="150" 
             Width="400">
    <UserControl.Resources>
        <SolidColorBrush x:Key="BuildBorder.InProgressOrSucceeded.Border" 
                         Color="#FFAAFF00" />
        <SolidColorBrush x:Key="BuildBorder.Stopped.Border" 
                         Color="#FFFFFF00" />
        <SolidColorBrush x:Key="BuildBorder.Failed.Border" 
                         Color="#FFFF3300" />
        <Style x:Key="BuildBorderStyle" 
               TargetType="{x:Type Border}">
            <Setter Property="BorderThickness" 
                    Value="5 0 0 0" />
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#FF444444" />
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="BuildSucceededBorderStyle" 
               BasedOn="{StaticResource BuildBorderStyle}"
               TargetType="{x:Type Border}">
            <Setter Property="BorderBrush" 
                    Value="{StaticResource BuildBorder.InProgressOrSucceeded.Border}" />
        </Style>
        <!-- ReSharper disable once Xaml.RedundantResource -->
        <Style x:Key="BuildInProgressBorderStyle" 
               BasedOn="{StaticResource BuildSucceededBorderStyle}"
               TargetType="{x:Type Border}">
            <Style.Triggers>
                <Trigger Property="Visibility" Value="Visible">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation AutoReverse="True" 
                                                RepeatBehavior="Forever" 
                                                Storyboard.TargetProperty="BorderBrush.Color" 
                                                Duration="00:00:01" 
                                                To="Transparent" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- ReSharper disable once Xaml.RedundantResource -->
        <Style x:Key="BuildFailedBorderStyle" 
               BasedOn="{StaticResource BuildBorderStyle}"
               TargetType="{x:Type Border}">
            <Setter Property="BorderBrush" 
                    Value="{StaticResource BuildBorder.Failed.Border}" />
        </Style>
        <!-- ReSharper disable once Xaml.RedundantResource -->
        <Style x:Key="BuildStoppedBorderStyle" 
               BasedOn="{StaticResource BuildBorderStyle}"
               TargetType="{x:Type Border}">
            <Setter Property="BorderBrush" 
                    Value="{StaticResource BuildBorder.Stopped.Border}" />
        </Style>
        <Style x:Key="TextBlockStyle"
               TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" 
                    Value="#FFFFFFFF" />
            <Setter Property="TextWrapping" 
                    Value="WrapWithOverflow" />
        </Style>
        <Style x:Key="ProjectTextBlockStyle" 
               BasedOn="{StaticResource TextBlockStyle}" 
               TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" 
                    Value="16" />
            <Setter Property="FontWeight" 
                    Value="SemiBold" />
        </Style>
        <Style x:Key="MessageTextBlockStyle" 
               BasedOn="{StaticResource TextBlockStyle}" 
               TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" 
                    Value="16" />
        </Style>
        <Style x:Key="OverseerTextBlockStyle" 
               BasedOn="{StaticResource TextBlockStyle}" 
               TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" 
                    Value="10" />
            <Setter Property="FontWeight" 
                    Value="SemiBold" />
        </Style>
    </UserControl.Resources>
    <Border>
        <Border.Style>
            <MultiBinding Converter="{StaticResource BuildStatusToStyleConverter}" ConverterParameter="Build{0}BorderStyle">
                <Binding RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=UserControl}"/>
                <Binding Path="Status" />
            </MultiBinding>
        </Border.Style>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0"
                        Margin="20 10">
                <TextBlock x:Name="Project" 
                           Style="{StaticResource ProjectTextBlockStyle}" />
                <TextBlock x:Name="Message" 
                           Style="{StaticResource MessageTextBlockStyle}" />
            </StackPanel>
            <StackPanel Grid.Row="2"
                        Margin="20 10">
                <TextBlock Style="{StaticResource OverseerTextBlockStyle}">Overseer</TextBlock>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>