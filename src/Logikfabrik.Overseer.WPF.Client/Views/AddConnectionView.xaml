<UserControl x:Class="Logikfabrik.Overseer.WPF.Client.Views.AddConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="400">
    <ScrollViewer Style="{StaticResource ScrollViewerDefaultStyle}">
        <StackPanel Margin="25">
            <ContentControl x:Name="Settings"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Focusable="False" />
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AddConnection" 
                        Content="OK" 
                        IsDefault="True"
                        Style="{StaticResource ButtonDefaultStyle}" 
                        Margin="0 0 2 0" />
                <Button x:Name="ViewConnections" 
                        Content="Cancel" 
                        Style="{StaticResource ButtonDefaultStyle}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>