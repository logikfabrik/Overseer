<UserControl
    x:Class="Logikfabrik.Overseer.WPF.Provider.TeamCity.Views.BuildProviderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Button
        x:Name="AddConnection"
        VerticalContentAlignment="Top"
        Style="{DynamicResource ButtonDefaultStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Viewbox
                Width="30"
                Height="30"
                Stretch="UniformToFill">
                <Path Fill="{Binding Path=(TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}" />
            </Viewbox>
            <TextBlock
                Grid.Row="1"
                Style="{DynamicResource TextBlockDefaultStyle}"
                Text="TeamCity"
                TextAlignment="Center" />
        </Grid>
    </Button>
</UserControl>
